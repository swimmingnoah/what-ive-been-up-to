services:
  web:
    build: .
    image: noah-portfolio:latest
    container_name: noah-portfolio
    restart: unless-stopped
    ports:
      - "8080:80"
    # Uncomment the volumes block for live editing without rebuilding.
    # Edit files in ./site and refresh the browser to see changes.
    # volumes:
    #   - ./site:/usr/share/nginx/html:ro
    #   - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
